version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
  env:
    - name: NAME
      value: "Yara"
    - name: MY_VAR_EXAMPLE
      value: "example"
run:
  command: python server.py
